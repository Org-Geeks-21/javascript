## Closer & SetTimeout Closer

 - **Closer is a function which bind the function with its lexical environment**

     **or**

 - **function along with lexical ennvironment is called closer**

 Note 
 ` when the function is removed from a lexical environment and then the function is evoked its variabled is referencing to the its lexical environment variable`

## SetTimeout 
 - **SetTimeout function is a function which callback the function and stores the function somewhere and set the timer provided**,
 **once timer expired it call back the function put it in the call stack and run the console**

 **SetTimeout closer**
 `it is a callback function in closer it remembers the value from the outer closer when the funtion is vanished`
