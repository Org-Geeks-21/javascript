# let & Const Declaration are hoisted in JS

 **define let**
 
 `let is used to declare a variable used in a block which can't be redeclare or hoisting can be done after initialisation`

**temporal dead zone**

`it is a time betweeen a let variable is hoisted and till it is initialise the value`

**define const**
`it is use to declare variable in block scope which can't be reassign or re intialise after frst initilisation`

| syntax error | type error | reference error |
| ------------ | ---------- | --------------- |
| it is nothing but a error in which the js can't understand the code | when a compiler try to do something with wrong type | when compiler try to use a variable that doen't exit in the current scope |
| ex:parancthesis,unexpected token | ex:calling something that is nota function | miss spelled variable names |

